# Scratch: reject-flow test PR

Throwaway PR used to verify that Quire's "reject" gesture actually closes the
underlying GitHub PR (see `closePullRequest` wiring in `src/engine/github`).

Safe to reject and delete — carries no real change.
